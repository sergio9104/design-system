import React from 'react';
import { Heading, GridItem } from '@chakra-ui/react';
import { ReactJSXElement } from '@emotion/react/types/jsx-namespace';

interface I{{pascalCase name}} {
  headline: string;
}

function {{pascalCase name}}({ headline = 'Title from {{pascalCase name}}' }: I{{pascalCase name}}): ReactJSXElement {
  return (
      <GridItem as="section">
        <Heading as="h2" color="white">{headline}</Heading>
      </GridItem>
  );
}

export default {{pascalCase name}};